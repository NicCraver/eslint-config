{
  "name": "@niccraver/eslint-config-monorepo",
  "version": "0.38.15",
  "private": false,
  "packageManager": "pnpm@8.3.0",
  "author": "NicCraver (https://github.com/NicCraver)",
  "license": "MIT",
  "scripts": {
    "lint": "pnpm -r run stub && eslint .",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "prepare": "pnpm -r run stub",
    "release": "bumpp -r && pnpm -r publish"
  },
  "devDependencies": {
    "niccravereslint-config": "workspace:*",
    "bumpp": "^9.1.0",
    "eslint": "^8.38.0",
    "eslint-plugin-nic": "link:./packages/eslint-plugin-nic",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
